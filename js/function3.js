// function3.js

// let friends = [];
// let i = 0; // 로컬 변수 : 실행할 때마다 배열이 하나씩 증가

function input_friend() {

    // let namef = document.getElementById('name');    //id 값으로 요소를 가져옴
    // let agef = document.getElementById('age').value;
    // let hobbyf = document.getElementById('hobby').value;

    // let friend = {} // object 생성
    // friend.name = namef.value;
    // friend.age = agef;
    // friend.hobby = hobbyf;

    // 1. 이름, 나이, 취미 값을 읽어옴
    // 2. td 태그 속에 값을 넣어준다
    // 3. tr 태그 에다가 td 태그를 append
    // 4. table에다가 tr append

    // let i = 0; // 지역 변수
    // friends[i++] = friend; // 호출 할때 마다 인덱스 값이 증가
    // console.clear(); 

    // for(friend of friends) {
    //     for(field in friend) {
    //         console.log(friend[field])
    //     }
    // }

    // console.log(namef.value, agef.value, hobbyf.value);
    // value = 초기값
    // document.getElementById('result').value = namef.value + ', ' + agef.value + ', ' + hobbyf.value;
    // document.getElementById('show').innerHTML = namef.value + ', ' + agef.value + ', ' + hobbyf.value;
    
}

